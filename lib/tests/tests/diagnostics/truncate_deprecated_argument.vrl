# DIAGNOSTICS
# result:
#
#  warning[E801]: the `ellipsis` argument is deprecated since v0.7.0
#        ┌─ :2:5
#        │
#      2 │ . = truncate("a", limit: 1, ellipsis: true)
#        │     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ this is deprecated
#        │
#        = try: the `truncate` function now supports a `suffix` argument.
#        =
#        =     The `suffix` argument can be used for appending arbitrary strings.
#        =
#        = see language documentation at https://vrl.dev
#        = try your code in the VRL REPL, learn more at https://vrl.dev/examples

. = truncate("a", limit: 1, ellipsis: true)
