# result:
# warning[E801]: the `to_timestamp` function is deprecated
#   ┌─ :2:1
#   │
# 2 │ to_timestamp!(5)
#   │ ^^^^^^^^^^^^^^^^ this is deprecated
#   │
#   = try: using another timestamp parsing function instead
#   =
#   =     for integer values, use `from_unix_timestamp`
#   =     for all other value types, use `parse_timestamp`
#   =
#   = see language documentation at https://vrl.dev
#   = try your code in the VRL REPL, learn more at https://vrl.dev/examples

to_timestamp!(5)
