# result:
#
# error[E635]: duplicate variable 'x' in loop pattern
#   ┌─ :2:8
#   │
# 2 │ for x, x in [1, 2] {
#   │        ^ duplicate variable 'x' in loop pattern
#   │
#   = see language documentation at https://vrl.dev
#   = try your code in the VRL REPL, learn more at https://vrl.dev/examples

for x, x in [1, 2] {
  .foo = x
}
