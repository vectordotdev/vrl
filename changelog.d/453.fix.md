Fixed a misleading `E103: unhandled fallible assignment` that was raised when an assignment was preceded in the same block by a discarded fallible expression (for example `set(e, [key], value)` followed by `output = push(output, e)`). The fallibility of the prior expression now produces an `E100: unhandled error` on the prior expression itself, instead of being attributed to the next assignment.
