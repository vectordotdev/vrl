Fixed `encode_proto` and `parse_proto` to support proto maps whose keys are not strings (`bool`, `int32`, `int64`, `uint32`, `uint64`, plus their `sint`/`fixed`/`sfixed` variants). Previously `parse_proto` errored with "Internal error while parsing protobuf map" and `encode_proto` silently dropped the field with a warning.
