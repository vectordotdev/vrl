The `example!` macro now works out of order and is guaranteed to work if and when optional fields in the `Example` struct are added. Therefore any changes to `Example` will not be considered breaking since users of VRL as a crate are expected to use the `example!` macro.

authors: thomasqueirozb
