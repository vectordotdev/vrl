Added an opt-in `execution_timeout` Cargo feature that lets embedders bound
how long a VRL program is allowed to run. When enabled and a timeout is
configured via `Runtime::set_timeout`, the program panics once wall-clock
execution time exceeds the deadline. This is a no-op unless the feature is
enabled and a timeout is set.

authors: gwenaskell