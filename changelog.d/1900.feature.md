Added an opt-in `execution_cancellation` Cargo feature that lets embedders
abort a running VRL program from another thread. When enabled and a flag is
registered via `Runtime::set_cancellation_flag`, the program checks the flag
on every expression resolution and panics as soon as it's observed set. This
is a no-op unless the feature is enabled and a flag is registered.

authors: gwenaskell