The compiler now reports every unhandled-error in a single compilation pass instead of stopping at the first one. Previously, a discarded fallible call followed by another error in the same program would only surface the latter — you'd fix it, recompile, and only then discover the first one.
