Add first-class `for` loops supporting single-item and key-value iteration over arrays and objects, native `break` and `continue` control flow statements, variable shadowing with in-place slot reuse, and compile-time collection validation.

authors: jimmystewpot
